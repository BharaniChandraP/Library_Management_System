Library Management System (Microservices)
This project is a fully containerized Library Management System built with a microservices architecture. Each service is decoupled and designed to handle specific business logic, communicating through a shared network and database.

How to Run
Run the following command:
docker-compose up -d

Access Points:
Main Application: http://localhost:3000
Service Endpoints: Ports 5001 - 5004
